spring:
  ai:
    dashscope:
      api-key: ${ALI_AI_KEY}
      chat:
        options:
          model: qwen-plus
      embedding:
        options:
          model: text-embedding-v4

logging:
  level:
    org.springframework.ai: debug
    io.modelcontextprotocol:
      client: debug
      spec: debug